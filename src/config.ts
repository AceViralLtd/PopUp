export interface Config {
    useDefaultStyle: boolean,
    closeButton: boolean,
    singlePopUp: boolean,
}

export const defaultConfig: Config = {
    useDefaultStyle: true,
    closeButton: false,
    singlePopUp: true,
};
